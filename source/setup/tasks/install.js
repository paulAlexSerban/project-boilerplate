/**
 * INSTALL task
 * 
 * 1. get and create docker containers
 * 2. configure docker containers
 * 3. remove unnecessary files form the docker containers
 */

console.log('install project dependencies and instances');
